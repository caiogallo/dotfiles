#!/usr/bin/env bash
# Integração com Android via KDE Connect (plugin Phone Connect do DMS)
# Nota: valent (backend GNOME alternativo) não está disponível no Arch; o kdeconnect já cobre.
dms plugins install dankKDEConnect